{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Capability for permission dialog windows",
  "windows": ["perm_*"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
